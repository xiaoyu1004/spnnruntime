#include "integral.h"

namespace spnnruntime
{
    // load layer specific parameter from parsed dict
    void Integral::loadParam(const ParamDict::ptr pd)
    {
    }

    DEFINE_LAYER_CREATOR(Integral)
} // spnnruntime